apiVersion: v1
kind: ConfigMap
metadata:
  name: portal-d-staging-config
  namespace: exam-4 # Trainee Dave's assigned namespace
data:
  # This block creates a file called 'config.json' inside the ConfigMap
  config.json: |
    {
      "app_name": "portal-d",
      "subtitle": "Staging Environment",
      "owner": "Dave"
    }
